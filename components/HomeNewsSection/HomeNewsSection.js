import React from 'react';
import styles from './HomeNewsSection.module.scss';

const HomeNewsSection = ({}) => {
    return (
        <div className={styles.HomeNewsSection}>HomeNewsSection</div>
    );
};

export default HomeNewsSection;